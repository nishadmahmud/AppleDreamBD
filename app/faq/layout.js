export const metadata = {
  title: "FAQ",
  description:
    "Find quick answers to common AppleDream questions about orders, shipping, returns, and product details.",
};

export default function FaqLayout({ children }) {
  return <>{children}</>;
}

