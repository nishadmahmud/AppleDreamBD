export const metadata = {
  title: "Contact AppleDream",
  description:
    "Have questions about devices, orders, or support? Reach the AppleDream team through our dedicated contact channels.",
};

export default function ContactLayout({ children }) {
  return <>{children}</>;
}

