export const metadata = {
  title: "Terms & Conditions",
  description:
    "Read the AppleDream terms of service covering purchases, site usage, liabilities, and user responsibilities.",
};

export default function TermsLayout({ children }) {
  return <>{children}</>;
}

